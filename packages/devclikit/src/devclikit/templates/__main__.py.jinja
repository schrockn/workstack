"""{{ cli_name }} entry point."""

from devclikit.cli_factory import create_cli

cli = create_cli(
    name="{{ cli_name }}",
    description="{{ description }}",
    commands_dir="{{ commands_dir }}",
    version="{{ version }}",
)

if __name__ == "__main__":
    cli()
